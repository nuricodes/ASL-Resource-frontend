import React from 'react'
import {Link} from 'react-router-dom'

const Header = (props) => {
    return (
    <nav>
        <Link to='/signup'>
        </Link>

        <Link to='/login'>
        </Link>
    </nav>
    )
}

export default Header